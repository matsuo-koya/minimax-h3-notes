# MiniMax H3 を速くするために、うちで試したこと全部 (2026-08-14 〜 09-05)

RTX 5090 (32GB) + WSL2 + ComfyUI 0.33 の自作スタジオ（LaViale）で、MiniMax H3（映像+音声を同時生成する
オープンウェイト動画モデル）を 3 週間回すあいだに入れた高速化と、その途中で分かったことの記録。
数字は全部この環境の実測。**由来**の欄は、どこから来た技術かを正直に書いた:

- ◇ **公式 / コミュニティ** — 配られていたものを入れて測った
- ★ **独自** — うちで見つけた／作った／初めて数字を出した
- ✗ **否定結果** — 入れようとして測ったら要らなかった・効かなかった（これも成果として残す）

---

## 0. まとめの表

| # | 手法 | 効き目（実測） | 由来 |
|---|---|---|---|
| 1 | int8 ConvRot 重み（Comfy-Org 配布） | 21GB で 5090 に載る。真の量子化誤差 **0.90%** | ◇ 重み / ★ 誤差の実測 |
| 2 | Turbo LoRA 8 step（公式蒸留） | 20 step 比 **2.2 倍**（154 秒 vs 339 秒）。日本語の焼き込みも読める | ◇ |
| 3 | FastH3 4 step（FastVideo の DMD2 蒸留 LoRA）を ComfyUI 形式に変換 | Turbo 8step 比 **1.87 倍**（118 秒 vs 225 秒）。顔・口パクは崩れない。本番 MV 30 区間で破綻ゼロ | ◇ LoRA / ★ **変換器**（qkv 対角ブロック・fc1 前後入替） |
| 4 | comfy-kitchen INT8 アテンション（ComfyUI 内蔵 `ModelAttentionBackend`） | サンプリング **約 2 倍**（99→54 秒 / 91→45 秒）。ArcFace・口パク相関は変わらず。4090 でも 1.34 倍 | ◇ 機能 / ★ H3 での実測・本番投入 |
| 5 | 幅を 1280 → 1216 に | VAE デコードのタイル 28→24、**合計 −15%**（124.8→105.9 秒） | ★ |
| 6 | 区間の分散生成（取りに来させる行列・5090 + 4090） | 28 区間 MV **3.8 → 2.7 時間**。同じ区分を続けて配って積み替え 210→55 秒 | ★ 設計 |
| 7 | 4090 の 345f の崖（7 本中 6 本が 3〜17 倍遅い）→ 上限 328f | 328f は 11 本すべて 882〜897 秒（ばらつき 1.7%） | ★（n=1 で決めた誤りの訂正） |
| 8 | H3-Optimizations の `H3MemoryOptimization` | **1.27 倍**（134→106 秒）。ただし**出力がビット一致しない**（画素差 2.27/255）。GGUF では動かない | ◇ ノード / ★ 非決定性・GGUF 不動の発見 |
| 9 | ToneCompensate（継ぎ目の色補正） | 偏りは **1/255 未満**・向きも揃わない → 入れない | ✗ |
| 10 | Sage attention / VSA（FastVideo の疎アテンション）/ 148GB 本体 | nvcc 無し・B200 前提で **うちでは動かない** | ✗ |
| 11 | alibaba-pai Acc-LoRA（PDD） | LoRA ではなく 32 ステップ並列解法の重み。ComfyUI に PDD が無いので使えない | ✗ |
| 12 | `/api/job` の様子見が生成を止めていた | 18〜43 秒の沈黙が消えた（生成中の ComfyUI は HTTP に応答しない） | ★ 運用 |
| 13 | ComfyUI は壊れる前に「劣化」する | クリップが **1.8 倍**遅くなったら再起動（CUDA 崩壊の前兆） | ★ 運用 |

**5090 単機の到達点**: 1280×704・5 秒強の 1 本が、素の 20 step（2.2 倍の逆算で約 4.5 分）→ Turbo 8step 125 秒
→ CK アテンション **79 秒**。参照つき 7.6 秒（リップシンク MV の 1 区間）は 127 秒 → **69 秒**。

---

## 1. 重み: int8 ConvRot（◇ / ★誤差の実測）

- `Comfy-Org/MiniMax-H3` の `*_pruned_int8_convrot.safetensors`（fl2va / ref2va 各 21GB）。
  5090 では staged のまま区間をまたいでも再ロードしない（`0 models unloaded`）
- ★ 変換の検算で、**int8 ConvRot の真の量子化誤差は 0.90%**（bf16 の元重みと比べて）。
  変換器のコメントにある 0.17% は「既に int8 格子の上にある重みを再符号化したときの数字」で、
  量子化誤差ではない。NF4 は同じ物差しで桁が違う
- 4090 (24GB) は Q4_K_M GGUF。**8 と 4 は同居できない話が多い**（§8 の GGUF 不動）

## 2. Turbo LoRA 8 step（◇）

公式の `minimax_h3_fl2v_turbo_8step` / `ref2v_turbo_4step`。ref2va には 4 step 版しか無いので
本番は **4step 版を 8 step で**回している（品質が上がる）。

- 20 step と比べて 2.2 倍速く、**日本語の焼き込み（歌詞を画面内の物として描かせる）も 8 step で読める**。
  20 step にしても可読性は変わらず時間だけ 2.2 倍
- H3 の派生マージ（zs05 = Z-Image の質感移植）にも**そのまま当たる**

## 3. FastH3 4 step — LoRA の変換器（◇ LoRA / ★ 変換）

FastVideo (hao-ai-lab) の `FastVideo-FastH3-4-step-Preview-v1-LoRA`（dense / data-free 版・
1.38GB・rank 64）は diffusers の鍵名で配られていて、ComfyUI にそのままでは当たらない。
`tools/h3_lora_convert.py` で変換した。

**対応表は実測で確定した**（lightx2v が同じ LoRA を diffusers 版と ComfyUI 版の両方で配っているのを
正解表にして、416 本すべてビット一致するまで総当たり）:

| 層 | 変換 |
|---|---|
| **qkv**（融合） | lora_A は `[A_q; A_k; A_v]`、lora_B は q,k,v 順の**対角ブロック** |
| **fc1**（gate/up 融合） | lora_B の**前半と後半を入れ替える**（diffusers `[value; gate]` → ComfyUI `[gate; value]`） |
| adaln 一族 | **当てられない**（pruned 本体は時刻埋め込みを `adaln_t_table [1025,8]` に畳んであり基底が違う）→ 理由つきで落とす。品質に出ない |

⚠ **fc1 の順序は shape が同じなので、間違えてもエラーが出ない。** 症状は
「作風は乗るのに顔（identity）だけ乗らない・強くすると崩壊・step が効かない」。
これは後日、Apple Silicon (MLX) 側で同じ LoRA を移植したときに出た謎の症状と**同じ原因**だった
（以来、手元の機械群では「連結重みは分割マージで順序まで実証してから採用」を決まりにした）。

### 実測（124f・1280×704・同一参照/駆動音声・3 シード）

| | 時間 | 口パク相関（ずれ 0） |
|---|---|---|
| Turbo 8step | 225〜236 秒 | +0.21 / +0.28（1 本は顔追跡不能） |
| **FastH3 4step** | **118〜123 秒（1.87 倍）** | **+0.36 / +0.37 / +0.53** |

- 顔は 3 シードとも参照画像の同一人物。t2av ベースからの蒸留で ref2va の口が崩れる心配は外れた
- scheduler は simple と beta で差なし
- 本番尺（158〜243f・30 区間）で破綻ゼロ。1 本 **約 105 秒**（20 step は 458 秒）

### ★ 副産物: FastH3 のブロック重みは H3 と同一

int8 ConvRot の検算中に判明。10 本すべてで「H3 vs FastH3 の差」が「int8 の量子化誤差」と
小数点以下 3 桁まで一致した。**4-step 蒸留はブロックの重み行列には入っていない。**
違うのは adaln だけ（うちの pruned は `[96768, 8]`、向こうの full は `[96768, 2688]`）。

さらに `adaln_t_table (1025, 8)` は H3 の time_embedder から作られた表で、
`z(i) = B · silu(temb(t))` が残差 2.4e-6 で成立する（行番号 i/1024 = t）。
⇒ pruned で学習した adaln LoRA は `ΔW_full = ΔW_pruned @ Z @ pinv(C)` で full-rank へ橋渡しできる
（MLX 側が実装・数理は成立を確認済み）。

⚠ 21GB を落とす前に **safetensors のヘッダだけレンジ取得**して鍵の形を見ること（99KB で済む）。

## 4. comfy-kitchen INT8 アテンション（◇ 機能 / ★ H3 での実測と本番投入）

きっかけは Reddit の「1 台の 5090 でエンドレス AI テレビ局」の記事。**ComfyUI 内蔵**の
`ModelAttentionBackend` ノード（attention = "comfy kitchen attention"）で、H3 の後段アテンション
（56 ヘッド × 128 次元）が INT8 カーネルに置き換わる。sageattention のインストールも nvcc も要らない。
sm_120 (5090) で `comfy_kitchen.int8_attention_is_available()` が True。

### マイクロベンチ（bf16・1×56×L×128）

| L (トークン) | SDPA | CK int8 | 倍率 |
|---|---|---|---|
| 15,448 | 28.7 ms | 10.8 ms | 2.7 |
| 30,000 | 108 ms | 40 ms | 2.7 |
| 60,000 | 431 ms | 158 ms | 2.7 |

相対誤差 1.6%。H3 のトークン数は `T = (frames−1)//17*5+2`、空間は /16 のあと 2×2 パッチなので
1280×704・5.2 秒 ≈ 32.5k、362f ≈ 94k。

### 実ジョブ（同一シード・pytorch → ck）

| ジョブ | サンプラー | 合計 |
|---|---|---|
| T2V 1280×704 5.2s 8step | 104.4 → **53.0 秒** | 124.8 → 78.9 秒 |
| I2V 1344×576 5.9s 8step・人物 LoRA 0.7（常設ディスプレイ用ループ） | 99.3 → **54.2 秒** | 116.9 → 69.4 秒 |
| LaViale ref2va 1280×704 7.6s fast4 | 91.0 → **44.8 秒** | 117.6 → 68.8 秒 |

- **品質**: ArcFace（実写 48 枚の重心との余弦）0.322 → 0.311（同一人物の線 0.28 を維持）、
  参照画像との一致 0.165 → 0.158、口パク相関は区間のばらつきの範囲内
- ⚠ **t2v は同じシードで別のサンプルになる**（量子化で乱数経路が変わる）。ref2va は参照が錨になるので
  ほぼ同じ絵（4090 側の実測 PSNR 26.2 / SSIM 0.954）
- 4090 (Q4_K_M) でも **1.34 倍**（207.6 → 154.6 秒・124f）。5090 より効きが小さいのは尺が短いため
  と見ている（アテンション単体の倍率は一定で、124f はアテンション以外の割合が大きい）
- 本番はワークフローを組み立てる側が環境変数 1 つで全経路（t2v / ref / 長尺）に
  `ModelAttentionBackend` を挿す。`--use-ck-attention`（ComfyUI 全体）は使っていない

### ノード別の内訳（★ プロファイラ）

`/history` は合計しか返さないので、ws の `executing` イベントの間隔からノード別に積む
46 行のプロファイラを書いた（`tools/profile_nodes.py`）。

| | サンプラー | VAE デコード | SaveVideo |
|---|---|---|---|
| pytorch | 84% | 11〜17% | 2% |
| ck | 65〜78% | 18〜31% | 3〜4% |

**アテンションを半分にすると VAE デコードが次のボトルネック**になる（20 秒前後・固定費）。

## 5. 幅 1280 → 1216（★）

VAE デコードのタイル数が 28 → 24 になり、**合計 −15%**（124.8 → 105.9 秒）。
画素は 5% しか減らない。H3 は縦横とも 32 の倍数なら何でも通る。

## 6. 区間の分散生成（★ 設計）

28 区間のリップシンク MV が 5090 だけで 3.8 時間かかるので、別マシンの 4090 にも分けた。
**投げるのではなく、向こうから取りに来させる**（claim / progress / result の 3 口）。相手は ComfyUI を
tailnet に開かずに済み、速い機械が自然に多く取る。5090 も同じ行列から取るが、ComfyUI のキューが
空のときだけ（対話的な生成を待たせない）。

- 実測 **1.4 倍**（3.8 → 2.7 時間）。取りに行く形なので比を設計に入れる必要が無い
- **同じ区分（ref / t2v / 静止画）を続けて配る**。跨ぐと 11GB の積み替えが挟まり、
  区間あたりの固定費が 55 秒 → 210 秒に戻る（`SEG_LAST[worker]`）
- 行列はディスクに控える（サーバーを再起動しても対応が消えない。消えて 1 本潰したことがある）
- 転んだ区間を貸出のまま放置しない（15 分固まった）。3 回で `failed`

## 7. 4090 の 345f の崖 — n=1 で上限を決めない（★）

| frames | 本数 | 最小 | 中央 | 最大 | ばらつき |
|---|---|---|---|---|---|
| 311 | 5 | 822 秒 | 822 秒 | 827 秒 | 0.6% |
| **328** | **11** | 882 秒 | 892 秒 | 897 秒 | **1.7%** |
| 345 | 7 | 491 秒 | 2860 秒 | **8626 秒** | **17 倍** |

最初は 1 本だけ測って 345 を上限にした。その 1 本が「7 本に 1 本の当たり」だった。
遅い run のメモリ行は正常な run と**完全に同一**（VRAM の飽和ではない）、退避ログも 0 件、
tqdm の積算と経過が 85 分ずれる（**サンプリングの外で消えている**）。原因未特定のまま 328 で止めてある。

⚠ 5090 には崖が無い（277→362f で 1 フレームあたり 1.40 → 1.71 秒と滑らか）。

## 8. H3-Optimizations（◇ / ★ 非決定性と GGUF 不動）

Zironic の `H3MemoryOptimization`（DiT → ノード → guider・scheduler）。

| | 素 | ノード有り |
|---|---|---|
| 3 本 | 131.1 / 131.5 / 140.1 秒 | **105.9 / 106.3 / 105.9 秒** |

- 1.27 倍（作者の 1.57〜1.69 倍には届かない）。ばらつきがほぼゼロになる
- ★ **出力がビット一致しない**（作者が書いていない）。素どうしは同じシードで 0.000 なのに、
  ノードを入れると平均画素差 2.267/255。髪と輪郭の微細部だけで人物・構図は同じ
- ★ **GGUF では動かない**: `mat1 and mat2 shapes cannot be multiplied (4096x5376 and 3024x14336)`。
  3024 = Q4_K_M が 256 要素を 144 バイトに詰めた「入れ物の形」（5376/256×144）。
  ノードが `weight.shape` をそのまま読むため。pruned が原因ではない
- スパースアテンション (KV 30%) は品質の代償が明記されているので入れていない

## 9. ToneCompensate は要らない（✗）

「H3 のデノイザーは色が偏り、続きのセグメントは暗くなる」という主張のノード。
重なりが同じ内容の 2 つの描画であることを使って偏りを測る発想は巧い。測ったら:

| 場面 | 輝度 | 偏り |
|---|---|---|
| 昼の部屋 | 83.1 | −0.42 (−0.5%) |
| 夜景 | 29.5 | **+0.84** (+2.8%) |
| 白い台所 | 119.5 | −0.25 (−0.2%) |

**絶対値はどれも 1/255 未満、向きも揃わない**（夜景は明るくなった）。当てると崩す恐れがあるので入れない。
⚠ コマ間の差では継ぎ目は測れない。重なりを直接比べて初めて分離できる（重なり区間の 2 つの描画を直接比べる）。

## 10. 使えなかったもの（✗）

- **Sage attention**: nvcc が無く入らない → CK で代替できた
- **FastVideo VSA 版 / 148GB 本体**: 自前 VSA-H3 カーネル・CUDA13・B200 前提。使えるのは dense LoRA だけ
- **alibaba-pai Acc-LoRA**: `pdd_num_steps: 32 / pdd_block_size: 4`、`proj_out.weight → (32, 96, 5376)`。
  「8 step」は 32 ステップを 4 個ずつ並列に解く (PDD) の意味で、普通の 8 step サンプラーとは別物。
  ComfyUI に PDD を実装しないと意味が無い（`Jalen-Brunson/ComfyUI-MiniMax-H3-PDD-Acc` が出ている・未検証）

## 11. 運用で速くなったもの（★）

- **`/api/job` の様子見が本業を止めていた**: 生成中の ComfyUI は HTTP に応答しないので、6 秒おきの
  ポーリングが毎回そこで詰まり、呼んだ側が timeout していた（18 / 40 / 43 / 22 秒の沈黙）。
  前回から 45 秒たったときだけ見る + `asyncio.wait_for` 4 秒
- **一度きりの通信エラーで 6 分のクリップを捨てていた**: 10 回連続で失敗するまで粘る
- **ComfyUI は壊れる前に劣化する**: 2 日連続稼働で CUDA unknown error 崩壊の前に、クリップ生成が
  1.8 倍遅くなり、同居プロセスが 25〜164 秒沈黙する。遠くのワーカーの claim タイムアウト連続が
  良いカナリア。`systemctl --user restart minimax-comfyui` で 20 秒で戻る
- **冷えた 1 本目で測らない**: Krea 2 静止画は 5090 で 29.3 秒（冷）→ 3.7 秒（温）。
  同じワークフローを 2 回投げるとキャッシュに当たって 0.1 秒になるのでシードを変える

---

## 測り方の決まりごと（3 週間で 4 回踏んだ）

1. **1 シード・1 本で決めない**（zs05 の構図、345f の崖、両方これで誤った）
2. **対照を必ず取る**（プロンプトだけでも ArcFace 0.133 出る。差を見ないと「顔が出た」とは言えない）
3. **冷えた 1 本目を捨てる**・**同シード 2 投目のキャッシュヒットを避ける**
4. **shape が同じでも順序は分からない**（fc1）。連結重みは分割マージで順序まで実証してから採用
5. **時間はノード別に測る**（合計だけだと VAE の固定費が見えない）

## 参照

- 変換器: `tools/h3_lora_convert.py`（`--compare` で公式 ComfyUI 版との一致検証）
- プロファイラ: `tools/profile_nodes.py`
- 重み: `Comfy-Org/MiniMax-H3`、LoRA: 公式 Turbo、`FastVideo/FastH3-4-step-Preview-v1-LoRA`
- ノード: `Zironic/H3-Optimizations`、`rkfg/ComfyUI-MiniMaxH3-ToneCompensate`、
  `NikoDemon80/ComfyUI-H3-Motion-Context`（速度ではなく継ぎ目の話・v0.3.1）
